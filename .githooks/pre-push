#!/usr/bin/env bash
set -euo pipefail

echo "Ejecutando tests antes del push..."
make test

echo "Checking Python coverage before push..."
make check-python-coverage COVERAGE_MIN="${COVERAGE_MIN:-90}"
